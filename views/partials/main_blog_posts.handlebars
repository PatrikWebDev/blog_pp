<div>
    <h1 style="text-align: center;">
        {{blogTitle}}
    </h1>
    <table class="table table-bordered table-hover">
        <thead class="thead-light">
        <tr>
                <th class="text-center">Title</th>
                <th class="text-center">Author</th>
                <th class="text-center">Date</th>
                <th class="text-center" >View</th>
            </tr>
    </thead>
        <tbody>
    <form method="POST" action="/postView">
    {{#each blogs}}
    <tr>
        <td class="text-center"> {{this.title}}</td>
            <td class="text-center"> {{this.author}}</td>
            <td class="text-center"> {{this.date}}</td>
            <td class="text-center">
    <button class="alert alert-primary" type="submit" name="title" value="{{this.title}}">
        View Bolg Post
    </button>
            </td>
    </div>
{{/each}}
    </ul>
    </form>
        </tbody>
    </table>
</div>